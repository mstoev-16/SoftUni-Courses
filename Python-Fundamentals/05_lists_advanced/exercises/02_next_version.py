# current_version = ''.join(list(map(str, input().split('.'))))
# new_version = '.'.join(str(int(current_version) + 1))
# print(new_version)

# current_version = int(''.join(input().split('.')))
# new_version = '.'.join(str(current_version + 1))
# print(new_version)

#
# strings = ''.join(input().split('.'))
# strings = int(strings) + 1
# print('.'.join(list(str(strings))))
